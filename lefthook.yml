pre-commit:
  commands:
    lint-staged:
      run: pnpm lint-staged

pre-push:
  commands:
    typecheck:
      run: pnpm run typecheck
    build:
      run: pnpm run build
    unit-tests:
      run: pnpm run test:unit
    spa-auth-tests:
      run: pnpm run test:spa:auth
