pre-commit:
  commands:
    repo-hygiene:
      run: pnpm run ci:check:repo-hygiene:staged
    lint-staged:
      run: pnpm lint-staged

pre-push:
  commands:
    local-gate:
      run: pnpm run ci:local-gate
